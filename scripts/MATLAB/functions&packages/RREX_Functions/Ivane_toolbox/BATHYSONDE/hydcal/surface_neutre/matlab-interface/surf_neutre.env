#
# Selon le systeme (solaris ou linux), l'executable C  de calcul de maree
# et les fichiers binaires sont differents.
# 
if ( -e '/boot/vmlinuz') then
	setenv NEUTRE_EXT .linux
else
	setenv NEUTRE_EXT .solaris
endif
